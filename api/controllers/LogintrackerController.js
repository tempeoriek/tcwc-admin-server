const Logintracker = require('../models/login_tracker');

LogintrackerController = {
  fetchRecord: async function (req, res) {
    response.ok(data, res, `request success`)
  },
};

module.exports = LogintrackerController;

